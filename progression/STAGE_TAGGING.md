# Stage tagging

Generated questions and historical paper metadata may declare `stage`, `learningStage`, or `frameworkStage`. Untagged legacy content defaults to `icas-y2` for backward compatibility. New content must be explicitly tagged before release so progression filtering is deterministic.
