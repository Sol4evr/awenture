import fs from 'node:fs';
import path from 'node:path';
import crypto from 'node:crypto';
import {fileURLToPath} from 'node:url';
import * as pdfjs from 'pdfjs-dist/legacy/build/pdf.mjs';
const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..');
const qa=JSON.parse(fs.readFileSync(path.join(root,'dist/stage-papers/auto-question-count-verification.json'),'utf8'));
const catalog=JSON.parse(fs.readFileSync(path.join(root,'dist/stage-papers/catalog.json'),'utf8'));
const bounds=JSON.parse(fs.readFileSync(path.join(root,'dist/stage-papers/auto-boundary-verification.json'),'utf8'));
const meta=new Map();for(const s of Object.values(catalog.stages||{}))for(const p of s.papers||[])meta.set(p.sourcePath,p);
const outDir=path.join(root,'dist/stage-paper-source-review');fs.rmSync(outDir,{recursive:true,force:true});fs.mkdirSync(outDir,{recursive:true});
function sha(abs){return crypto.createHash('sha256').update(fs.readFileSync(abs)).digest('hex')}
async function pageText(doc,n){const pg=await doc.getPage(n),tc=await pg.getTextContent();const parts=[];for(const it of tc.items||[]){const s=String(it.str||'').trim();if(s)parts.push(s)}return parts.join(' ').replace(/\s+/g,' ').trim()}
function numbers(text){const out=[];for(const m of String(text).matchAll(/(?:^|\D)(\d{1,3})(?=\D|$)/g)){const n=Number(m[1]);if(n>=1&&n<=100)out.push(n)}return [...new Set(out)].sort((a,b)=>a-b)}
const groups=new Map();for(const [src,e] of Object.entries(qa.papers||{})){if(e.questionCountVerified)continue;const abs=path.join(root,src);if(!fs.existsSync(abs))continue;const h=e.sha256||sha(abs);if(!groups.has(h))groups.set(h,[]);groups.get(h).push(src)}
const index=[];for(const [h,srcs] of groups){const src=srcs[0],p=meta.get(src),b=bounds.papers?.[src];if(!p||!b?.questionEndPage)continue;try{const abs=path.join(root,src),doc=await pdfjs.getDocument({data:new Uint8Array(fs.readFileSync(abs)),disableWorker:true,isEvalSupported:false,useSystemFonts:true}).promise;const end=Math.min(doc.numPages,b.questionEndPage),learner=[];for(let n=Math.max(1,end-3);n<=end;n++){const text=await pageText(doc,n);learner.push({page:n,text:text.slice(0,7000),numbers:numbers(text)})}const post=[];for(let n=end+1;n<=Math.min(doc.numPages,end+3);n++){const text=await pageText(doc,n);post.push({page:n,text:text.slice(0,7000),numbers:numbers(text)})}const id=h.slice(0,16),rec={id,sha256:h,sourcePaths:srcs,stage:p.stage,subject:p.subject,year:p.year,questionEndPage:end,totalPages:doc.numPages,learnerTail:learner,postBoundary:post};fs.writeFileSync(path.join(outDir,`${id}.json`),JSON.stringify(rec,null,2)+'\n');index.push({id,sha256:h,sourcePaths:srcs,stage:p.stage,subject:p.subject,year:p.year,questionEndPage:end,totalPages:doc.numPages,reviewFile:`stage-paper-source-review/${id}.json`});try{doc.destroy()}catch(_){}}catch(err){index.push({id:h.slice(0,16),sha256:h,sourcePaths:srcs,stage:p?.stage,subject:p?.subject,year:p?.year,error:String(err?.message||err)})}}
index.sort((a,b)=>String(a.stage).localeCompare(String(b.stage))||String(a.subject).localeCompare(String(b.subject))||Number(a.year||0)-Number(b.year||0));fs.writeFileSync(path.join(outDir,'index.json'),JSON.stringify({version:'aw-stage-pending-source-review-v1',generatedAt:new Date().toISOString(),unresolvedEntries:Object.values(qa.papers||{}).filter(e=>!e.questionCountVerified).length,uniqueBinaries:index.length,index},null,2)+'\n');console.log(JSON.stringify({release:'6.18.2',pendingSourceReviewExport:'PASS',unresolvedEntries:Object.values(qa.papers||{}).filter(e=>!e.questionCountVerified).length,uniqueBinaries:index.length}));
