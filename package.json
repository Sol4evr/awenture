{
  "name": "awenture",
  "version": "6.11.0",
  "private": true,
  "scripts": {
    "validate:bank": "node scripts/validate-expansion.mjs",
    "build": "npm run validate:bank && node scripts/build.mjs",
    "test:release": "npm run build && node tests/release-gate.mjs",
    "test:browser": "playwright test",
    "test": "npm run test:release && npm run test:browser"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0"
  }
}
